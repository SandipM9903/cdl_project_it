import { BASE_URL } from "../config/Config";

// Define the configuration objects
const config = {


  ITDeclarationUrl: `${BASE_URL}`,
};

const adminConfig = {
  ITDeclarationAdminUrl: `${BASE_URL}`,
};

// Export the objects
export { adminConfig, config };
