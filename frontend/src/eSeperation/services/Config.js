import { BASE_URL } from "../../config/Config";

const config = {
    loginUrl: `${BASE_URL}:9028/check`,
    profileCardsUrl: `${BASE_URL}:9010/api`,
    empSeparationUrl: `${BASE_URL}:9029/api/eSeparation`,
    announcementUrl: `${BASE_URL}:9090/api/announcement`,
  };
  
  export default config;
